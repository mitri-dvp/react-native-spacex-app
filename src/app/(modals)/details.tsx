import { View, Text } from "react-native";
import React from "react";

const LaunchDetailModal = () => {
  return (
    <View>
      <Text style={{ textAlign: "center" }}>Launch Details</Text>
    </View>
  );
};

export default LaunchDetailModal;
